# POST /api/products/:id/ratings - Add a rating/review

# GET /api/products/:id/ratings - Get all ratings for a product

# PUT /api/ratings/:id - Update a rating (owner only)

# DELETE /api/ratings/:id - Delete a rating (owner or admin only)